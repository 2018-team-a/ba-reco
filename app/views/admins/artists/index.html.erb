<div class="container">
<div class="table-responsive">
  <table class="table">
    <thead>
    	<h1 class="h1">登録アーティスト一覧</h1>
      <tr>
        <th>アーティスト名</th>
      </tr>

      <% @artists.each do |f| %>
      <tr>
        <td><%= link_to f.name, admins_artist_path(f.id) %></td>
      </tr>
      <%end%>
    </thead>
  </table>
  <h3><%= link_to "登録ページ", new_admins_artist_path %></h3>
</div>
</div>
